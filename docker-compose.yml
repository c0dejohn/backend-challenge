version: '3.7'
services:
  postgres:
    image: postgres:11
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_DB=disney
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
    volumes:
      - ./postgres-volume:/data/db
